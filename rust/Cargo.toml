[package]
name = "rust_file"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]

libc = "0.2"

# json
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
